// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://votre-username.github.io',
  // base: '/bubblestone-website', // Décommenter pour GitHub Pages
  output: 'static',
});
