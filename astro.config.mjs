// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://florentcoulonbs.github.io',
  base: '/Bubblestone.ai',
  output: 'static',
});
