---
interface FAQItem {
  question: string;
  answer: string;
}

const faqs: FAQItem[] = [
  {
    question: "Quels types d'entreprises accompagnez-vous ?",
    answer: "Nous travaillons principalement avec des PME innovantes, prêtes à embrasser l'IA pour gagner en compétitivité. Des entreprises qui veulent aller vite, qui comprennent que l'automatisation n'est plus une option mais un levier de croissance. Peu importe votre secteur — ce qui compte, c'est votre volonté d'avancer."
  },
  {
    question: "Combien coûtent vos prestations ?",
    answer: "Nos interventions sont facturées entre 600€ et 800€ la journée, selon la complexité du projet et les compétences mobilisées. Pas de surprise : on cadre ensemble le périmètre, on estime le nombre de jours, et on s'y tient. Chaque euro investi doit générer un retour mesurable — c'est notre engagement."
  },
  {
    question: "Que peut-on faire avec un Agent IA ?",
    answer: "Un Agent IA travaille pour vous, 24h/24, sans fatigue ni oubli. Il peut gérer des processus métiers complets de bout en bout : qualifier des leads, enrichir des données, rédiger des rapports, surveiller vos systèmes, répondre à vos clients, orchestrer des workflows complexes entre vos outils. Ce qui prenait des heures à une équipe devient instantané et systématique."
  },
  {
    question: "Pourquoi vous ?",
    answer: "Plus de 20 ans d'expérience terrain dans l'IT : infrastructure, cloud, dev, data, marketing digital et automatisation. On a l'expérience, on sait ce qui est robuste, ce qui ne l'est pas et ce qui doit être fait. Aujourd'hui, on met cette expertise au service de l'IA appliquée — pas pour suivre la tendance, mais pour vous donner un avantage concret et durable."
  },
  {
    question: "Comment se déroule une mission type ?",
    answer: "1. Audit — On analyse votre existant, vos outils, vos irritants. 2. Étude de faisabilité — On identifie ce qui peut être automatisé et le ROI attendu. 3. PRD (Product Requirements Document) — On formalise la solution, les étapes, les livrables. 4. Implémentation IA — On construit, on teste, on déploie. 5. Maintenance & évolution — On reste présents pour ajuster, améliorer et faire évoluer."
  }
];
---

<section id="faq" class="faq section-stone">
  <div class="faq-texture"></div>
  <div class="container">
    <div class="faq-header-wrapper">
      <div class="faq-glow"></div>
      <div class="section-header">
        <span class="section-badge">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/>
            <line x1="12" y1="17" x2="12.01" y2="17"/>
          </svg>
          FAQ
        </span>
        <h2>Questions fréquentes</h2>
        <p>Tout ce que vous devez savoir sur nos services et notre accompagnement.</p>
      </div>
    </div>

    <div class="faq-list">
      {faqs.map((faq, index) => (
        <details class="faq-item" data-index={index}>
          <summary class="faq-question">
            <span>{faq.question}</span>
            <div class="faq-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="12" y1="5" x2="12" y2="19"/>
                <line x1="5" y1="12" x2="19" y2="12"/>
              </svg>
            </div>
          </summary>
          <div class="faq-answer">
            <p>{faq.answer}</p>
          </div>
        </details>
      ))}
    </div>

    <div class="faq-cta">
      <p>Vous avez d'autres questions ?</p>
      <a href="#contact" class="btn btn-secondary">
        Contactez-nous
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .faq {
    padding: var(--spacing-3xl) 0;
    position: relative;
    z-index: 1;
  }

  /* Stone texture overlay */
  .faq-texture {
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.7' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
    opacity: 0.035;
    pointer-events: none;
  }

  /* Top edge highlight */
  .faq::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, rgba(148, 163, 184, 0.4) 50%, transparent 100%);
    z-index: 1;
  }

  /* Bottom edge shadow */
  .faq::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, rgba(0, 0, 0, 0.3) 50%, transparent 100%);
  }

  .faq .container {
    position: relative;
    z-index: 100;
  }

  /* Wrapper avec effet halo pour le header - style stone */
  .faq-header-wrapper {
    position: relative;
    max-width: 700px;
    margin: 0 auto var(--spacing-xl) auto;
    padding: var(--spacing-xl);
    background: var(--gradient-stone-surface);
    backdrop-filter: blur(8px);
    border: 1px solid var(--stone-border);
    border-top-color: rgba(148, 163, 184, 0.2);
    border-radius: var(--radius-xl);
    overflow: hidden;
    z-index: 100;
    box-shadow: var(--stone-shadow-inset);
    transition: border-color 0.5s ease, box-shadow 0.5s ease;
  }

  .faq-header-wrapper::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(180deg, rgba(148, 163, 184, 0.08) 0%, transparent 100%);
    pointer-events: none;
  }

  .faq-header-wrapper .section-header {
    margin-bottom: 0;
    background: transparent;
    padding: 0;
  }

  .faq-header-wrapper .section-header h2 {
    color: var(--color-white);
  }

  .faq-header-wrapper .section-header p {
    color: var(--color-gray-300);
  }

  .faq-header-wrapper .section-badge {
    background: rgba(59, 130, 246, 0.2);
    border: 1px solid rgba(59, 130, 246, 0.3);
  }

  .faq-header-wrapper.line-reached {
    border-color: rgba(59, 130, 246, 0.6);
    box-shadow:
      0 0 20px rgba(59, 130, 246, 0.4),
      0 0 40px rgba(59, 130, 246, 0.2),
      0 0 60px rgba(59, 130, 246, 0.1),
      inset 0 1px 0 rgba(59, 130, 246, 0.2);
  }

  .faq-glow {
    display: none;
  }

  .faq-list {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .faq-item {
    position: relative;
    background: var(--gradient-stone-surface);
    backdrop-filter: blur(8px);
    border: 1px solid var(--stone-border);
    border-top-color: rgba(148, 163, 184, 0.2);
    border-radius: var(--radius-xl);
    overflow: hidden;
    transition: all var(--transition-slow);
    z-index: 100;
    box-shadow: var(--stone-shadow-inset);
  }

  .faq-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(180deg, rgba(148, 163, 184, 0.08) 0%, transparent 100%);
    pointer-events: none;
  }

  .faq-item:hover {
    border-color: rgba(59, 130, 246, 0.6);
    box-shadow:
      0 0 20px rgba(59, 130, 246, 0.4),
      0 0 40px rgba(59, 130, 246, 0.2),
      0 0 60px rgba(59, 130, 246, 0.1),
      inset 0 1px 0 rgba(59, 130, 246, 0.2);
  }

  .faq-item[open] {
    border-color: rgba(59, 130, 246, 0.6);
    box-shadow:
      0 0 25px rgba(59, 130, 246, 0.5),
      0 0 50px rgba(59, 130, 246, 0.25),
      0 0 75px rgba(59, 130, 246, 0.1),
      inset 0 1px 0 rgba(59, 130, 246, 0.3);
  }

  .faq-question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    cursor: pointer;
    list-style: none;
    font-weight: 500;
    color: var(--color-white);
    transition: all var(--transition-base);
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-question span {
    font-size: 1rem;
  }

  .faq-icon {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(59, 130, 246, 0.1);
    border: 1px solid rgba(59, 130, 246, 0.2);
    border-radius: var(--radius-md);
    color: var(--color-accent);
    flex-shrink: 0;
    transition: all var(--transition-base);
  }

  .faq-item:hover .faq-icon {
    background: rgba(59, 130, 246, 0.15);
    border-color: rgba(59, 130, 246, 0.3);
  }

  .faq-item[open] .faq-icon {
    background: var(--gradient-primary);
    border-color: transparent;
    color: white;
    transform: rotate(45deg);
    box-shadow: 0 0 15px rgba(59, 130, 246, 0.4);
  }

  .faq-answer {
    padding: 0 1.5rem 1.5rem;
    animation: faq-open 0.3s ease;
  }

  @keyframes faq-open {
    from {
      opacity: 0;
      transform: translateY(-8px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .faq-answer p {
    font-size: 0.9375rem;
    line-height: 1.7;
    color: var(--color-gray-300);
    padding-top: 1rem;
    border-top: 1px solid rgba(148, 163, 184, 0.2);
  }

  .faq-cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin-top: var(--spacing-xl);
    padding: var(--spacing-xl) var(--spacing-lg);
    border-top: 1px solid rgba(148, 163, 184, 0.2);
    position: relative;
    z-index: 100;
    background: transparent;
  }

  .faq-cta p {
    font-size: 1rem;
    color: var(--color-gray-300);
  }

  @media (max-width: 768px) {
    .faq-header-wrapper {
      padding: var(--spacing-lg) var(--spacing-md);
    }

    .faq-question {
      padding: 1rem 1.25rem;
    }

    .faq-question span {
      font-size: 0.9375rem;
    }

    .faq-answer {
      padding: 0 1.25rem 1.25rem;
    }
  }
</style>
